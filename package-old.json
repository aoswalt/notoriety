{
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "tsc --noEmit && eslint '*/**/*.{js,ts,tsx}' --quiet --fix"
  },
  "dependencies": {
    "gray-matter": "^4.0.2",
    "jest": "^24.9.0",
    "ts-jest": "^24.0.2",
  }
}
